# Checkout foundation (legacy note)

`enquiry_orders` remains for WhatsApp / callback leads.

**Real online orders** are in migration `004_orders.sql` (`orders`, `order_items`, `order_events`) with checkout at `/porosia` and Stripe webhook at `/api/webhooks/stripe`.
